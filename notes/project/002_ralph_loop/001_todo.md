1. We need to always ensure that, whenever the chatbot, that lives inside of 'justralph.it', push the issues in the github repo, and whenever the ralph loop session begins, 'it pulls' those issues. Always. All the time. This is non-negotiable. 

2. Ralph should be able to receive files (maybe attached by the user in the chatbot) in an /upload folder, whether images or text files. Beads has an option to get attachments. 

3. The only reasons the ralph loop should stop is when it requires human intervention; this means no ambiguity should arrive to the ralph loop. ever. this is something that the step 1) iteration process with the chatbot living inside of justralph.it should handle. Ralph will always assume everything is right and ready to implement, meaning that no contradictions should be inside the PRD.

4. 