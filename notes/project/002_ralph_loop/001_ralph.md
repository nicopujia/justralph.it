1. For this demo we'll be using sonnet 4.6 for every task via openrouter.
2. We have mostly all developed inside of: pkgs/ralph & pkgs/bd


## Goal

The goal is to: 

- Correctly engineer a way to enroute this system with each layer of communication with Ralph, the user's prompts, and the output he receives when Ralph is executing (just showcase what openroute is loading)
- Ensure ralph is executing everything to perfection. Polish it enough so every to-do performed is correctly going directly to code execution-mode rather than second-guessing or creating unnecessary slop.
- Ensure the beads layer is precisely wrapped with Ralph (need to understand this a lot better)